/**
 * Access a property on an object.
 * @param {number|string} key The key you want
 * @param {object} data The object you want to extract to key from
 */
export default function dot (key, data) { return data[key] }
